package main

// Version is the current semantic version of GWiM.
//
// Bump via `make version-increment` or directly when releasing.
// The Makefile keeps git tags and the .app bundle in sync with this value.
const Version = "2.0.10"
